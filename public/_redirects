# Cloudflare Pages Redirects
# Handle SEO redirects and URL normalization

# IMPORTANT: Static files must be served directly (not through SPA)
# Cloudflare Pages automatically serves files from /public, but we need
# to ensure the SPA fallback doesn't catch these paths

# Redirect www to non-www (uncomment if you want this)
# https://www.agrilink.com.ng/* https://agrilink.com.ng/:splat 301

# Redirect old waitlist URL if it existed
/waitlist /  301

# Redirect any trailing slashes (except root)
/home/ /home 301
/login/ /login 301
/signup/ /signup 301

# Handle common typos or variations
/farmer /farmers-signup 301
/buyer /buyer-signup 301
/join /  301
/register /signup 301
/signin /login 301

# SPA fallback - MUST be last
# This catches all non-file routes and serves index.html
/* /index.html 200
